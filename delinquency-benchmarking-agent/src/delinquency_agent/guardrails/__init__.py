"""Guardrails: entitlements + suppression (data-path) and numeric grounding (output).

These are enforced in code, not requested in a prompt, so they cannot be
prompted away. See docs/06-governance.md.
"""
